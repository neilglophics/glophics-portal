/** Tailwind v4 as a PostCSS plugin — this is what replaces the runtime CDN
 *  compile the old public/index.html used. Same tokens, now built. */
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
